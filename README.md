Steps to transfer to a new machine:

```
$ git clone git clone https://github.com/stsievert/dotfiles.git
$ cd dotfiles
$ bash deploy.sh
$ bash nvim/pylsp.sh  # might require editing for local directory structure
```
